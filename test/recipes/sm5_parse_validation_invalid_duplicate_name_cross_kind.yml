version: 1
steps:
  - kind: add_temp
    name: shared_name
    handles: [tmp0]

  - kind: apply_rules
    name: rewrite_stage
    rules:
      - name: shared_name
        match:
          opcode: frc
          rewrite_mode: none
