version: 1
steps:
  - kind: apply_rules
    name: invalid_immediate_token_kind
    rules:
      - match:
          opcode: mul
        emit:
          - opcode: mov
            operands:
              - type: immediate32
                immediates_u32:
                  - bad: token
