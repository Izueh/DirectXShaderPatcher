version: 1
steps:
  - kind: add_temp
    name: add_tmp_conflict
    handle: tmp0
    handles: [tmp1]

  - kind: apply_rules
    name: followup_apply_rules
    rules:
      - name: rule
        match:
          opcode: mov
          rewrite_mode: none
