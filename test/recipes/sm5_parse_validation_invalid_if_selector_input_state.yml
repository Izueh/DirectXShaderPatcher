version: 1
steps:
  - kind: apply_rules
    name: invalid_if_selector
    if:
      state: probe_ok
      input: enable_noise_patch
    rules:
      - name: rule
        match:
          opcode: mul
        emit:
          - opcode: mov
