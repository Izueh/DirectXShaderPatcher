version: 1
steps:
  - kind: apply_rules
    name: invalid_replay_object_capture_empty
    rules:
      - name: rule
        match:
          opcode: mul
          operands:
            - capture: dst
        emit:
          - opcode: mov
            operands:
              - capture: { from: "" }
