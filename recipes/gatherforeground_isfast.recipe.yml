version: 1

options:
  restore_reflection: true

resources:
  textures:
    - id: added_texture
      name: MyTex
      kind: texture_2d
      element: F32
      width: 4
      binding:
        bind: auto
        space: 0

steps:
  - kind: add_texture
    id: added_texture
