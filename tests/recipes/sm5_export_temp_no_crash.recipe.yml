version: 1
steps:
  - kind: apply_rule
    name: export_temp_check
    rewrite_mode: none
    rule:
      match:
        - opcode: frc
          operands:
            - capture: temp_op
              export_as: temps
