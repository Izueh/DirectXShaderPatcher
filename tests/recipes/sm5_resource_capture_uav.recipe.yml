version: 1
steps:
  - kind: apply_rule
    name: capture_uav_access
    rewrite_mode: none
    rule:
      match:
        - opcode: ld
          operands:
            - type: temp
            - type: temp
            - type: resource
              capture: uav_op
              export_as: uav_access
