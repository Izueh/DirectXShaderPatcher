# Archived workflows

clang-x64 / clang-x86 compile-compat smoke builds. Disabled from running:
GitHub Actions only picks up workflows from `.github/workflows/`. Restore by
moving the file(s) back into `.github/workflows/`.
